/**
 * @param {string} s
 * @return {boolean}
 */
const isValid = (s) => {
    const stack =[];
    
    for (let i=0;i<s.length; i += 1){
        const top = stack 
    }
    
};

module.exports = { isValid };


